use structure :: defining_and_instanting;
use structure :: tuple;
use structure :: area_rectange;
fn main() {
    println!("Hello, world!");
    defining_and_instanting();
    tuple();
    area_rectange();
}
